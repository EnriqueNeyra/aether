# Aether custom partition table (4 MB ESP32-C3)
#
# ESPHome's stock 4 MB Arduino layout gives each OTA slot 0x1C0000 (1,835,008 B)
# and then leaves 0x60000 (384 KB) of flash completely unallocated at the end.
# HomeKit (HomeSpan + SRP/HAP crypto) does not fit in what was left of the stock
# app slot, so that dead space is reclaimed and split evenly across app0/app1,
# taking each slot to 0x1F0000 (2,031,616 B).
#
# NOTE: changing the partition table invalidates OTA from older Aether builds.
# Devices running <= 1.0.12 must be re-flashed once over USB with the factory
# image; OTA works normally again for every release after that.
#
# Name,     Type, SubType, Offset,   Size,
nvs,        data, nvs,     0x9000,   0x5000,
otadata,    data, ota,     0xE000,   0x2000,
app0,       app,  ota_0,   0x10000,  0x1F0000,
app1,       app,  ota_1,   0x200000, 0x1F0000,
eeprom,     data, 0x99,    0x3F0000, 0x1000,
spiffs,     data, spiffs,  0x3F1000, 0xF000,
